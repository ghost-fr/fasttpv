<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:FastTPV.Desktop.ViewModels"
        x:Class="FastTPV.Desktop.Views.SalesWindow"
        x:DataType="vm:SalesWindowViewModel"
        Title="Sales / POS Module"
        WindowState="Maximized"
        Background="#F5F5F5">

    <Grid RowDefinitions="60,*,Auto" ColumnDefinitions="300,*,380">
        <!-- Header -->
        <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Background="#2D2D2D" Padding="20,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="🛒 Sales Point of Sale" FontSize="18" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding StatusMessage}" FontSize="12" Foreground="#B0B0B0" VerticalAlignment="Center" Margin="20,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- Left Panel: Search and Filter -->
        <Border Grid.Row="1" Grid.Column="0" Background="White" BorderThickness="0,1,1,0" BorderBrush="#E0E0E0" Padding="15">
            <StackPanel>
                <TextBlock Text="Search / Scan" FontWeight="Bold" Margin="0,0,0,10"/>
                <TextBox x:Name="SearchBox" Watermark="Search by name or code, or scan a barcode..." Padding="10" Margin="0,0,0,10"
                         Text="{Binding SearchText}" KeyDown="SearchKeyDown"/>

                <TextBlock Text="Categories" FontWeight="Bold" Margin="0,15,0,10"/>
                <ListBox ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}" Height="150"/>

                <Separator Margin="0,15,0,15"/>

                <TextBlock Text="Customer" FontWeight="Bold" Margin="0,0,0,10"/>
                <ComboBox ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer}"
                          HorizontalAlignment="Stretch">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </StackPanel>
        </Border>

        <!-- Center Panel: Products Grid -->
        <Border Grid.Row="1" Grid.Column="1" Background="White" BorderThickness="0,1,1,0" BorderBrush="#E0E0E0" Padding="15">
            <StackPanel>
                <TextBlock Text="Available Products" FontWeight="Bold" Margin="0,0,0,10"/>
                <ScrollViewer>
                    <ItemsControl ItemsSource="{Binding FilteredProducts}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#F9F9F9" BorderThickness="1" BorderBrush="#E0E0E0" CornerRadius="8" Padding="15" Width="180" Margin="5">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" TextAlignment="Center" TextWrapping="Wrap" Margin="0,0,0,5"/>
                                        <TextBlock Text="{Binding Price, StringFormat='{}{0:C}'}" FontSize="16" FontWeight="Bold" Foreground="#4CAF50" TextAlignment="Center" Margin="0,0,0,5"/>
                                        <TextBlock Text="{Binding StockLevel, StringFormat='Stock: {0}'}" FontSize="12" Foreground="#666" TextAlignment="Center" Margin="0,0,0,10"/>
                                        <Button Content="Add to Cart" Background="#4CAF50" Foreground="White" Padding="10"
                                                HorizontalAlignment="Stretch" HorizontalContentAlignment="Center"
                                                Command="{Binding $parent[ItemsControl].((vm:SalesWindowViewModel)DataContext).AddToCartCommand}"
                                                CommandParameter="{Binding}"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </StackPanel>
        </Border>

        <!-- Right Panel: Shopping Cart -->
        <Border Grid.Row="1" Grid.Column="2" Background="White" BorderThickness="0,1,0,0" BorderBrush="#E0E0E0" Padding="15">
            <Grid RowDefinitions="Auto,*,Auto">
                <TextBlock Grid.Row="0" Text="Shopping Cart" FontWeight="Bold" Margin="0,0,0,10"/>

                <ScrollViewer Grid.Row="1">
                    <ItemsControl ItemsSource="{Binding CartItems}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#F9F9F9" CornerRadius="6" Padding="10" Margin="0,0,0,8">
                                    <StackPanel>
                                        <TextBlock Text="{Binding ArticleName}" FontWeight="Bold"/>
                                        <Grid ColumnDefinitions="Auto,Auto,Auto,*,Auto" Margin="0,5,0,0">
                                            <Button Grid.Column="0" Content="-" Width="28" Padding="0"
                                                    Command="{Binding $parent[ItemsControl].((vm:SalesWindowViewModel)DataContext).DecrementLineCommand}"
                                                    CommandParameter="{Binding}"/>
                                            <TextBlock Grid.Column="1" Text="{Binding Quantity}" Width="30" TextAlignment="Center" VerticalAlignment="Center"/>
                                            <Button Grid.Column="2" Content="+" Width="28" Padding="0"
                                                    Command="{Binding $parent[ItemsControl].((vm:SalesWindowViewModel)DataContext).IncrementLineCommand}"
                                                    CommandParameter="{Binding}"/>
                                            <TextBlock Grid.Column="3" Text="{Binding LineTotal, StringFormat='{}{0:C}'}" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,10,0"/>
                                            <Button Grid.Column="4" Content="✕" Width="28" Padding="0" Foreground="#FF5252" Background="Transparent" BorderThickness="0"
                                                    Command="{Binding $parent[ItemsControl].((vm:SalesWindowViewModel)DataContext).RemoveLineCommand}"
                                                    CommandParameter="{Binding}"/>
                                        </Grid>
                                        <Grid ColumnDefinitions="*,Auto" Margin="0,6,0,0">
                                            <TextBox Text="{Binding DiscountPercentText}" Watermark="Disc %" FontSize="11" Padding="4"/>
                                            <Button Grid.Column="1" Content="Apply %" FontSize="11" Padding="8,4" Margin="4,0,0,0"
                                                    Command="{Binding $parent[ItemsControl].((vm:SalesWindowViewModel)DataContext).SetLineDiscountCommand}"
                                                    CommandParameter="{Binding}"/>
                                        </Grid>
                                        <TextBlock Text="{Binding DiscountAmount, StringFormat='Discount: -{0:C}'}" FontSize="10" Foreground="#DC2626"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <StackPanel Grid.Row="2" Margin="0,10,0,0">
                    <Separator Margin="0,0,0,10"/>
                    <Grid ColumnDefinitions="*,Auto" Margin="0,0,0,8">
                        <TextBox Text="{Binding TicketDiscountPercentText}" Watermark="Whole-ticket discount %" Padding="8"/>
                        <Button Grid.Column="1" Content="Apply" Padding="12,8" Margin="6,0,0,0" Command="{Binding ApplyTicketDiscountCommand}"/>
                    </Grid>
                    <Grid ColumnDefinitions="120,*">
                        <TextBlock Grid.Column="0" Text="Subtotal:"/>
                        <TextBlock Grid.Column="1" Text="{Binding Subtotal, StringFormat='{}{0:C}'}" Foreground="#666" HorizontalAlignment="Right"/>
                    </Grid>
                    <Grid ColumnDefinitions="120,*" Margin="0,5,0,5">
                        <TextBlock Grid.Column="0" Text="Ticket discount:"/>
                        <TextBlock Grid.Column="1" Text="{Binding TicketDiscountAmount, StringFormat='-{0:C}'}" Foreground="#DC2626" HorizontalAlignment="Right"/>
                    </Grid>
                    <Grid ColumnDefinitions="120,*" Margin="0,0,0,5">
                        <TextBlock Grid.Column="0" Text="Tax:"/>
                        <TextBlock Grid.Column="1" Text="{Binding Tax, StringFormat='{}{0:C}'}" Foreground="#666" HorizontalAlignment="Right"/>
                    </Grid>
                    <Grid ColumnDefinitions="120,*">
                        <TextBlock Grid.Column="0" Text="Total:" FontWeight="Bold"/>
                        <TextBlock Grid.Column="1" Text="{Binding Total, StringFormat='{}{0:C}'}" FontSize="16" FontWeight="Bold" Foreground="#4CAF50" HorizontalAlignment="Right"/>
                    </Grid>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Footer: Payment Options -->
        <Border Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Background="#2D2D2D" Padding="15" BorderThickness="0,1,0,0" BorderBrush="#1a1a1a">
            <Grid ColumnDefinitions="Auto,Auto,Auto,*,Auto,Auto">
                <Button Grid.Column="0" Content="💰 Cash" Padding="20,10" Background="#4CAF50" Foreground="White" FontWeight="Bold" Margin="0,0,10,0"
                        Command="{Binding CompleteSaleCommand}" CommandParameter="Cash"/>
                <Button Grid.Column="1" Content="💳 Card" Padding="20,10" Background="#2196F3" Foreground="White" FontWeight="Bold" Margin="0,0,10,0"
                        Command="{Binding CompleteSaleCommand}" CommandParameter="Card"/>
                <Button Grid.Column="2" Content="📱 Digital" Padding="20,10" Background="#FF9800" Foreground="White" FontWeight="Bold"
                        Command="{Binding CompleteSaleCommand}" CommandParameter="Digital"/>
                <Button Grid.Column="4" Content="🔄 Clear Cart" Padding="20,10" Background="#FF5252" Foreground="White" FontWeight="Bold" Margin="0,0,10,0"
                        Command="{Binding ClearCartCommand}"/>
                <TextBlock Grid.Column="5" Text="{Binding LastTicketNumber, StringFormat='Last ticket: {0}'}"
                           Foreground="#B0B0B0" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Window>
